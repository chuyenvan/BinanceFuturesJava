# TRẠNG THÁI & BƯỚC TIẾP — 2026-07-23

## 1. ĐÃ LÀM (tóm tắt)

### A. WFO Worst-N (roadmap notebooklm) — HOÀN TẤT, kết luận rõ
- Sửa 4 bug roadmap (`=1` vs `=true`, wire `SELECTOR_TOPN`, decouple gate, Kaggle không nhận extra_env → Oracle-only).
- Chạy N3/N5/N8 (mode B) + hard-SL sweep 0.06/0.10/0.15 trên Oracle (16 window OOS).
- **Verdict: thesis Worst-N FAIL OOS.** %OOS-dương chỉ 12–37%, edge dồn 1 window (2025Q4). Breadth N (3/5/8) không đổi kết luận.
- **Exit là lever, không phải selector breadth:** hard-SL **0.15 tốt nhất** — BURN 7→4, maxDD_mtm 16.6%→6.4%, WFE giữ 0.285. Nhưng **vẫn FAIL** ngưỡng pre-reg (WFE≥0.5, OOS+≥70%). SL chặt (0.06/0.10) giết edge.

### B. Điều tra data-integrity — SẠCH
- **Timezone: chỉ sai HIỂN THỊ**, epoch lưu UTC đúng. Đã harden: pin GMT+7 tường minh cho path ĐỌC Aerospike (byte-identical trên node GMT+7 như 226).
- **Ghost pre-listing: không đáng ngại** — chỉ 1 outlier /2674 clamp.
- **EXIT-CLAMP-118 (2674 case): BÌNH THƯỜNG** — 99.7% thoát trong ngày, fill-tại-open khi gap xuyên trailing-stop (slippage bảo thủ, đúng). Tập trung ở coin volatile (LUNA/SHIB/DOGE). KHÔNG phải bug.
- `Final PriceTP: null` = artifact thứ tự log (cosmetic), lệnh vẫn fill đúng.

### C. Simulator production standalone (226) — A/B best-N vs Worst-N
- `SELECTOR_INVERT=1` (production, in-sample) ≈ baseline (**chỉ ~6% thấp hơn**) → **selector là nguồn PnL PHỤ**; lời chủ yếu từ market-signal best-N + DCA + trailing.
- Bản "35k lệnh / +$50.6k" = **bộ tham số cũ aggressive** (nới gate: MIN_MOM 0.0172, PREDICT_MAX 0.197, AI_DYN_MIN 0.146...). Nhiều lệnh hơn → in-sample đẹp hơn nhưng **overfit-prone** (đã bị revert về bảo thủ trước đó).

## 2. TRẠNG THÁI HẠ TẦNG
- **226**: jar `target/binance-java-sdk-1.2.4.jar` = build mới (TZ-harden + SELECTOR knobs). Jar cũ backup `*.bak_pretz_`. Truy cập: `ssh -p 2222 root@103.157.218.226` (key id_rsa_chuyennd).
- **Oracle (161.118.212.3)**: WFO exit-sweep xong, đang idle.
- Code sửa (chưa commit): `Configs.java` (SELECTOR_TOPN, SELECTOR_ONLY_ENTRY, TZ pin), `SimulatorMarketLevelTicker1MStopLoss.java` (cap N + SELECTOR_ONLY_ENTRY + log), `DataManagerAerospikeFloatSim.java` (TZ pin read).

## 3. KẾT LUẬN CHIẾN LƯỢC (thẳng)
- Worst-N selector: **ngõ cụt** (fail OOS + là lever phụ trong production). Dừng.
- Nút thắt thật đã định vị: **exit management (hard-SL/trailing/time-stop) + regime** (BURN dồn ở bear 2022).
- Câu hỏi tiền-thật còn mở: **chiến lược production (best-N + DCA + trailing) có robust OOS không?** — CHƯA test (mọi WFO đã chạy đều là nhánh worst-N/mode-B).

## 4. BƯỚC TIẾP (ưu tiên)
1. **[Chính] Production WFO OOS**: build WFO dataset từ selector production `funding_selector_pred_1m_v2` → chạy StrategyWfoTask `SELECTOR_INVERT=0`, DCA ON, `ABLATION_MODE=A`. WFO tự search 17 gene (gồm các param 14k↔35k) → tìm bộ **robust OOS**, không cãi in-sample. Kèm sweep exit (hard-SL≈0.15, TIME_STOP) trong hoặc sau.
2. **[Bổ trợ] Regime filter**: chặn entry khi BTC/breadth downtrend mạnh (cắt cụm BURN bear-2022) — thử sau khi có production WFO baseline.
3. **[Vặt] Vá cosmetic log** `Final PriceTP` (gộp vào lần build sau, log-only).

> Khuyến nghị: làm (1). Cần build dataset production-selector trước (setup ~30-60'), rồi WFO 16 window (~vài giờ Oracle). Đây là câu trả lời đúng cho "chiến lược thật sống OOS không".
