# HANDOFF — đọc ĐẦU TIÊN ở session mới (2026-08-23, bridge chết → reset)

> Session cũ mất bridge (lỗi sandbox Anthropic). Doc này để session mới tiếp tục liền mạch. Đọc kèm: `preregistration_frame_v1_2026-08-23.md` (khung đã đóng băng), `hpo_wfo_findings_2026-08-23.md`, `baseline_seal_jar_rootcause_2026-08-23.md`, `rebuild_leakfree_2sided_spec_2026-08-23.md`, `runbook_shadow_off_trade_2026-08-23.md`, và code `claude/code/cpcv_validation.py`.

## 0. USER PREFERENCES (nhắc lại)
Trả lời tiếng Việt, giữ term English. KHÔNG nịnh, chỉ lỗi/rủi ro trước. Thiếu info quan trọng thì hỏi thẳng; giả định nhỏ thì nêu rồi tiếp. Logging: Java SLF4J / Python logging / Node logger (cấm print/System.out). Model agent: KHÔNG Fable; Opus cho task mơ hồ/nhiều bước/side-effect, Sonnet cho việc cơ học. User đề cao PHẢN BIỆN kể cả khi đã chốt.

## 1. TRUY CẬP (verify lại khi vào — bridge hay chết)
- **Bridge**: desktop-commander MCP chạy trên WSL Windows máy user → ssh. HAY CHẾT (mất `mcp__remote-devices__*`). Nếu mất → không cứu được, phải session mới.
- **Oracle** (nguồn dataset + backtest/HPO): `ssh -i /root/.ssh/ora_key -o StrictHostKeyChecking=no ubuntu@161.118.212.3`. Chạy qua process WSL bash (desktop-commander start_process bash.exe). Script dài: ghi `/mnt/c/Users/pc/_x.sh` rồi `tr -d '\r' < /mnt/c/Users/pc/_x.sh | ssh ... 'bash -s'`. MCP cap 60s → job nặng chạy nohup + poll file.
- **Live 242**: `ssh -i /root/.ssh/ora_key -p 2222 root@103.157.218.242`. Process live: `BinanceOrderTradingManager` (PID đổi), launcher `/home/chuyennd/java/v_t_m/bin/daemon.sh` (source conf/env.sh). CẤM đụng/kill.
- **Repo device**: `E:\educa\source\github\20260415\BinanceFuturesJava`, mount `$HOME/mnt/BinanceFuturesJava`, qua `mcp__remote-devices__device_bash`. ⚠️ device_bash KHÔNG xoá được file (mv vào `_to_delete/`); mọi lệnh git để lại `.git/*.lock` → `mv .git/*.lock _to_delete/` sau mỗi git op. **Ưu tiên để USER làm git trên Windows** (đã gây detached-HEAD 1 lần vì checkout cần xoá file).
- **Kaggle**: CLI `/home/ubuntu/kaggle_latest_venv/bin/kaggle` (source ~/envs/xgb-env/bin/activate). Workers `wfo-worker-1..5` (`/home/ubuntu/claudedata/.run/kernels/`), datasets `java-run-lc`, `wfo-ds-xval`, `wfo-ticker-2021..2026pf`. Datasets KHÔNG version-pin → worker lấy latest. jobstore = aerospike `161.118.212.3:3222 ns=test`.

## 2. TRẠNG THÁI CHỐT (đã xong, đáng tin)
- **Baseline = 22687.6** (local Oracle, 18 window WFO 3-tháng OOS 2022→2026, jar `binance-fresh-20260809.jar` = KHỚP LIVE 96% class, StrategyWfoTask identical). Kaggle tái hiện **22595 (lệch 0.4%)** sau khi thay jar cũ (`1.2.4-shaded`, 0% khớp) → jar là root cause vụ 17k-vs-20k trước đó.
- **Rolling WFO (per-fold HPO N=30, non-frozen)**: chỉ **1/18 fold SUCCESS** (w8 WFE=0.59), tổng OOS ~8.5k << frozen 22.7k → **nghi overfit/leak MẠNH**. NHƯNG có caveat lớn: bị nhiễu bởi FITNESS (mục 3).
- **Data**: ds_base `/home/ubuntu/cmp/ds_base` (funding.bin 5.3G md5 779e2f8e, market/pred). Ticker corpus `/home/ubuntu/java/simulator/kaggle_data_hpo/daily` = 2008 file tới **2026-07-01**, corpus_md5 **b329fa06** (đã extend từ aerospike Oracle-local, đủ cho 2 fold 3-tháng 2026: w16=+1888.7, w17=+558.0).

## 3. PHÁT HIỆN LỚN NHẤT — FITNESS bóp nghẹt "nuôi"
`HPOFitnessCalculatorV4.java`: objective = Calmar (netPnl/maxDD) nhưng gác bởi **constraint CỨNG (reject = −100000)**. 2 luật cứng **cấm chiến lược "nuôi lãi"**:
- `TOO_MUCH_CAPITAL_LOCK` (line 227): `pctHeldOver7d` > ngưỡng → reject THẲNG dù đang lãi (bằng chứng: config +1031 PnL/234 trades bị fit=−100003).
- `TOO_FEW_TRADES` (line 120/211): `minTrades=max(5, windowDays×0.33)`≈30/quý → ép tần suất cao = lướt.
  → **Fitness STRUCTURALLY ép "lướt", cấm "nuôi"** → giải thích cả "model đánh lướt" lẫn rolling "no-edge" (HPO chỉ được chọn trong góc lướt). Đây là lý do phải làm lại objective (mục 5).

## 4. LEAK ĐÃ AUDIT
- WFO framework KHÔNG có purge/embargo. Label gate 15m (horizon 15m, grid 1m → overlap 93%); `train_gate_fold.py` KHÔNG đọc GATE_PURGE_MS dù WFOGateRunner truyền → **gate LIVE đang leak biên** (L1 trong docs/insights/WFO_LEAKS_TODO.md, chưa fix trên jar live).
- Selector 4h: predwf gen (`gen_funding_wf_predictions.py`) CÓ embargo (purge 72h) → OK. Label 1-chiều `y=maxFav>=6%` (bỏ downside → đuổi pump).
- Selector pred **BAKED vào funding.bin lúc BUILD** (`ExportWfoDataset`); `WFO_FUNDING_PRED_DIR` chỉ dùng lúc build, KHÔNG lúc backtest → đổi selector = phải rebuild funding.bin 5.3G (đắt). Label .pb `/home/ubuntu/claudedata/wfo15m/label_ds_15m/funding_label_15m_*.pb` CÓ đủ maxFav/maxAdv/tHitFav/tHitAdv (2-chiều sẵn sàng, decoder `/home/ubuntu/sel1m_code/funding_label_pb.py`).

## 5. KHUNG ĐÃ ĐÓNG BĂNG (pre-registration v1) — HƯỚNG ĐI ĐÃ CHỐT
File: `preregistration_frame_v1_2026-08-23.md`. Ý tưởng: dựng "đấu trường trung thực" rồi KHÔNG chỉnh bằng mắt (mọi lần nhìn-OOS-rồi-sửa = leak-qua-não). Quyết định đã khoá (user duyệt):
- Thời gian: **EXPLORE ≤2024** (tự do) · **TEST 2025→nay** (chạm 1 lần) · **FORWARD=shadow** (trọng tài).
- **O = median_fold(Calmar_net) − 0.5·std_fold(Calmar_net)**, net đủ fee+slippage+funding; BỎ reject cứng (funding tự tính phí việc giữ, không cấm).
- Sống-còn: maxDD-cap **70%**, leverage **1x**.
- PASS (quyết trước): `PBO<0.2 & DSR>0.95 & median WFE≥0.5 & %fold dương≥80%`.
- Knob tune (ít bậc tự do): `L_TP[0.04,0.10], L_SL[0.015,0.05]` (label 2-chiều, horizon FIX 4h); `TP[0.01,0.06], TS_MULT[1.0,6.0], MAX_HOLD_H[4,240]` (trục lướt↔nuôi). Fix phần còn lại + XGB hyperparam.
- CPCV N=8 k=2 (28 path), purge+embargo=horizon, knob chọn ở INNER, OUTER chỉ đo, đếm mọi trial.
- Shadow: giờ 1 nhánh (log live hiện tại); thêm nhánh 2 khi CPCV đẻ ứng viên.

## 6. ĐÃ BUILD (device-independent, đã test)
`claude/code/cpcv_validation.py` — CPCV+purge/embargo + DSR + PBO(CSCV), self-test PASS (28 path 0-leak; DSR deflate theo n_trials; PBO noise 0.67 vs edge 0.00). Sẵn sàng ghép pipeline.

## 7. GIT BRANCHES (device, CHƯA push)
- `module` = **13e7fd1** (branch user; đã có docs/project-memory + docs/runbooks). **85 file WIP của user** chưa commit (nguyên vẹn, chỉ modified-unstaged — ĐỪNG commit hộ).
- `fix/exchange-info-offline-env` = b09f52e (ClientSingleton đọc EXCHANGE_INFO_PATH từ env).
- `feat/leakfree-2sided-rebuild` = f7658eb (4 commit: gate embargo GATE_PURGE_MS; selector label 2-chiều triple-barrier env-param [maxAdv ÂM → adv_hit=maxAdv≤−SEL_ADV_PCT]; SELECTOR_GRID_MIN+SEL_SAMPLE_MODE nonoverlap; predwf nonoverlap train-only).
- `docs/project-memory-sync` = 2a2fc45.

## 8. VIỆC TIẾP THEO (khi bridge sống lại)
1. (nhẹ) git-commit đóng băng khung v1 + cpcv_validation.py vào repo (làm trên Windows nếu device-git lằng nhằng).
2. **Vá leak nền**: purge/embargo=horizon ở WFO buildWindows (StrategyWfoTask) + `train_gate_fold.py` (đọc GATE_PURGE_MS) + `feature_order.json` 1 nguồn. (một phần đã ở branch feat/leakfree).
3. **Thêm knob MAX_HOLD_H** (exit quá-hạn-giữ) vào sim `SimulatorMarketLevelTicker1MStopLoss` + expose L_TP/L_SL/TP/TS_MULT.
4. **Objective O mới** (soft, median−0.5σ Calmar-net, bỏ reject cứng) — sửa HPOFitnessCalculatorV4, cần recompile jar (build của user chạy Windows `build_fontfix.bat` → mvn -DskipTests package shade; Claude KHÔNG chạy .bat Windows → nhờ user build hoặc build cloud rồi verify reproduce).
5. **CPCV harness**: ghép cpcv_validation.py vào pipeline; chạy EXPLORE≤2024 debug (KHÔNG kết luận) → pre-register → TEST 2025 MỘT LẦN → DSR/PBO.
6. **Shadow** (USER HAND, runbook có sẵn): 242 `conf/env.sh` SHADOW_NO_PUSH=false→true + `bin/daemon.sh restart`. An toàn: chỉ chặn entry mới, vị thế mở vẫn đóng thật (reconcile từ Binance mỗi giây). Log `[SHADOW] would-BUY` ở `/home/chuyennd/java/v_t_m/logs/`.

## 9. HARD RULES (bắt buộc)
- **Oracle CHỈ 1 job compute** cùng lúc (4 job từng làm treo → reboot). CORE.md thực tế cho ≤3 nhưng session này user siết = 1.
- **Deploy/restart 2 process live = CHỈ user tay.** CẤM kill BinanceOrderTradingManager/ingestor/aerospike.
- Local WFO: `WFO_SMART_CACHE=0 + -Xmx16g`, KHÔNG 4yr-train (thrash → treo Oracle).
- device_bash không xoá được → mv `_to_delete/`; git device để lại lock → mv; ưu tiên git trên Windows.
- KHÔNG ghi output ra ổ C máy user (crash MCP). Dùng /home/ubuntu (Oracle) hoặc workspace cloud.

## 10. NHỚ KIỂM khi vào session mới
- `mcp__claude-code-remote__list_triggers` — vụ rolling WFO trigger đã done; xem còn trigger nào lơ lửng.
- Verify Oracle idle (`pgrep -af java`), disk (`df -h /home/ubuntu`), aerospike-wfo docker up (port 3222).
- Verify shadow đã bật chưa (242 env.sh SHADOW_NO_PUSH).