# REPORT 151 [TEAM SUCCESS] — Tối ưu PHẦN 2: nuôi lãi

## Bối cảnh đã rà trước khi chạy (tránh làm lại nhầm)
- Đọc `docs/MASTER_STRATEGY_CAMPAIGN.md` + `docs/STRATEGY_ROADMAP_3PART.md` (Desktop spec chủ), `docs/reports/141.md`
  + `docs/reports/142.md` (2 task liên quan đã DONE trước khi 3-team song song bắt đầu).
- **Phát hiện quan trọng:** sweep giveback "sơ 12h" trước đó (`/home/ubuntu/claudedata/giveback_oracle_master.md`
  + `giveback2_master.md`, chạy 2026-07-10 12:40-13:58) dùng **`WFO_DATA_DIR=wfo_dataset_v3`** — SAI dataset so
  baseline hiện chốt (`wfo_dataset_v4` = ret2, dùng ở report 141/142). Đây đúng là lý do task yêu cầu "làm lại
  đầy đủ" — sweep cũ không so sánh trên cùng baseline với cấu hình khuyến nghị.
- Jar dùng cho report 141 (`/home/ubuntu/java/simulator/binance-futures-backfill.jar`, build 2026-07-11 06:40)
  đã có sẵn `HARD_STOP_LOSS_RATE`/`TIME_STOP_HOURS`/`TS_GIVEBACK_RATIO` là config runtime (đọc từ
  `config.properties`, mặc định tắt/0.5). Đây là baseline code tôi dùng lại (KHÔNG đổi).

## Cách ly (HÀNG RÀO)
- Thư mục riêng `/home/ubuntu/team_success/simulator/` (KHÔNG dùng chung với team_entry/team_fail).
- Do cần đo thêm field mới (xem dưới) nên **KHÔNG symlink jar chung** — build jar riêng (superset, thêm 2 file)
  rồi scp thành file riêng trong thư mục cách ly. Không đụng/ghi đè jar chung `/home/ubuntu/java/simulator/`.
- pgrep rỗng trước khi build; xác nhận không job Java khác dùng path của tôi trước khi chạy.

## Code thêm (nhỏ, ĐO LƯỜNG ONLY — không đổi quyết định vào/ra lệnh)
Chỉ tiêu PRIMARY "% của đỉnh giữ được" chưa có field nào track (chỉ có `maeLow` cho đáy, không có đỉnh).
Thêm đối xứng, không ảnh hưởng SL/TP:
- `OrderTargetInfoTest.java`: field `maePeak` (đỉnh giá cao nhất từ entry, chỉ tăng) + update trong
  `updatePriceByKlineSimple` (song song `maeLow`).
- `PeakRetentionReport.java` (mới, package `bigchange.test`): đọc lại `storage/OrderTestDone.data` (không
  chạy lại sim), với lệnh THẮNG (`calProfit()>0`) tính `retention = pnlThucTe / (maePeak-entry)*qty`, in
  avg theo `MarketLevelChange`.
- Build local `mvn -q -o -DskipTests package` PASS, scp vào thư mục cách ly.

## Việc đang chạy
- Script `run_151_giveback.sh` (trong `/home/ubuntu/team_success/simulator/`), sweep
  `TS_GIVEBACK_RATIO ∈ {0.7, 0.85, 1.0}` trên `WFO_DATA_DIR=/home/ubuntu/claudedata/wfo_dataset_v4`
  (ret2, đúng baseline "Phần 1 cố định"), `SIM_END_DATE=20260601`, DCA mặc định + stops mặc định TẮT
  (không set `HARD_STOP_LOSS_RATE`/`TIME_STOP_HOURS`/`DISABLE_PREDICT_SYMBOL` → giữ hành vi cũ, đúng
  "Phần 3 cố định = DCA mặc định, stops off").
- PID launcher: xem `pgrep -af run_151_giveback` trên Oracle (161.118.212.3, user ubuntu, key
  `id_rsa_chuyennd`). Log tổng: `/home/ubuntu/claudedata/task151_giveback_v4.md`. Log từng mức:
  `/home/ubuntu/team_success/simulator/logs/sim_giveback_{0.7,0.85,1.0}.log`.
- Ước lượng: mỗi mức ~9-11 phút (theo timing sweep cũ) → tổng ~30 phút cho 3 mức, tuần tự (tránh
  vượt RAM-budget Oracle khi 3 team cùng chạy — Xmx6g/run).
- Verify khi xong: đọc `task151_giveback_v4.md` — có đủ 3 block `GIVEBACK=X done`, ladder năm/6th/quý
  từ `ladder_analyze.py`, PST/DCA_LEVEL1 breakdown từ `TraceData2Test`, %-đỉnh-giữ từ `PeakRetentionReport`.

(sẽ cập nhật tiếp khi có kết quả)
