# TASK-106 — Re-export Tool1 features (40 cột + EntrySignalFilter), per-month distributed

## Trạng thái: 66/66 DONE (queue `tool1_export_tasks_v1` trên Aerospike 226)

- 0 FAILED, 0 PENDING/RUNNING.
- Mỗi tháng 1 file `ff_YYYYMM.bin.gz` trong `features_export_python_v3/`.

## ⚠️ Output nằm ở 2 NƠI — phải gộp cả 2 mới đủ bộ 66 tháng

Lần chạy này dùng **6 worker**: 5 trên Kaggle + **1 chạy tay trên 226**.

### 1) Kaggle — 57 tháng (5 kernel, đều COMPLETE)
Slug: `chuyendinh/ff106-w1 .. ff106-w5`. Output trong `/kaggle/working/features_export_python_v3/`.
Tải:
```bash
for k in ff106-w1 ff106-w2 ff106-w3 ff106-w4 ff106-w5; do
  kaggle kernels output chuyendinh/$k -p /d/claudedata/dl-$k
  cp -n /d/claudedata/dl-$k/features_export_python_v3/*.bin.gz /d/claudedata/funding-ff-full/
done
```

### 2) Server 226 — 9 tháng (worker chạy tay)
Path: `/home/chuyennd/java/simulator/features_export_python_v3/`
Các tháng: `202103, 202309, 202211, 202305, 202502, 202401, 202602, 202506, 202601`.
Tải:
```bash
scp -i /c/Users/pc/.ssh/id_rsa_chuyennd -P 2222 \
  'root@103.157.218.226:/home/chuyennd/java/simulator/features_export_python_v3/ff_*.bin.gz' \
  /d/claudedata/funding-ff-full/
```

### Gộp + kiểm đủ
```bash
mkdir -p /d/claudedata/funding-ff-full
# ... (chạy 2 khối tải ở trên) ...
ls /d/claudedata/funding-ff-full/ff_*.bin.gz | wc -l   # PHẢI = 66
```

## VALIDATE (cổng — chưa làm, BẮT BUỘC trước khi dùng cho TASK-039)

Lần chạy này **bỏ qua smoke gate**, nên phải validate tổng sau khi gộp đủ 66 file:

1. **Đủ 66 file**, không thiếu tháng nào (2021-01 → 2026-06).
2. **Format**: mỗi record = 170 bytes (40 float + key/ts), `size % 170 == 0`, đọc thử 40 cột.
3. **Filter đã áp**: tổng dung lượng ~3–7 GB (giảm từ ~60 GB), %giữ ~10%, cross-sectional ~10% coin/mốc.
4. **Recompute ≥3 feature** từ Aerospike 226 → khớp (bằng chứng đúng, không chỉ chạy được).
5. **No-leak**: không có feature dùng dữ liệu tương lai.
6. **Bounded columns**: không NaN/Inf, biên hợp lý.
7. **Regime coverage**: có 2022 (crash) + 2024–25 (bull).
8. **★ Nhất quán 226-vs-Kaggle**: 9 tháng từ 226 phải cùng jar/commit với 57 tháng Kaggle.
   - Kiểm md5 jar: jar đẩy Kaggle (`binance-java-sdk-1.2.4-shaded.jar` trong dataset `java-run-lc`) vs jar worker 226 đã dùng.
   - Nếu lệch commit → 9 tháng 226 nghi ngờ, phải re-export bằng đúng jar Kaggle.

## Ghi chú thiết kế
- Claim race-safe bằng generation lock (`EXPECT_GEN_EQUAL`), `STALE_RUNNING_MS=30 phút` cho worker chết.
- Rủi ro đã quan sát: 5 worker đọc 226 đồng thời ở các tháng nặng (2026, ~780 coin) có thể nghẽn 226. Lần sau cân nhắc giảm concurrency cho tháng nặng.
- Code: `ExportTool1Master` (enqueue), `ExportTool1Worker` (claim/export/markDone) trong `ai_ml/features/export/fundingv2/`.
