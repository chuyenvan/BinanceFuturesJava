# NIGHT RUNBOOK 2026-07-02 → 03 (night-operator headless thực thi, STOP-on-FAIL)

## Nguyên tắc CỨNG
- Mọi bước ghi log /d/claudedata/night_*.log + append tiến độ vào /d/claudedata/BAO_CAO_SANG_20260703.md.
- FAIL/diff≠0/exception/mơ hồ/disk Oracle <5GB → DỪNG CHUỖI, ghi NEEDS_HUMAN + lý do vào báo cáo sáng. KHÔNG sáng tạo.
- Jobstore strategy_window Oracle = độc quyền: chỉ đụng SAU khi marker ~/claudedata/task115_A_DONE tồn tại trên Oracle.
- CẤM: đụng server 242, xoá dữ liệu, force-push, git add ., đổi tiêu chí pre-registered.
- SSH Oracle: ssh -i /c/Users/pc/.ssh/id_rsa_chuyennd ubuntu@161.118.212.3 (lọc: | grep -vE "post-quantum|store now|upgraded|openssh")

## Chuỗi (tuần tự)
1. **Chờ A_DONE** (poll 60s, tối đa 6h): file ~/claudedata/task115_A_DONE trên Oracle.
2. **Lưu + so cặp V4:** scp ~/claudedata/wfo_leaked_v1_report.md → docs/reports/wfo_leaked_funding_v1_report.md.
   Sinh docs/reports/wfo_pair_v4_compare.md: bảng side-by-side per-window (WFE, oosPnl, maxDD, trades) leaked vs
   leak-free (nguồn: report leaked + docs/reports/wfo_leakfree_funding_v2_report.md) + 3 metric verdict, gắn nhãn
   THAM KHẢO (V4 che số LOW_TRADES + confound coverage 8-16x, xem funding_coverage_audit_20260702.md). Commit.
3. **GATE-112** theo tasks/112 mục GATE (đọc kỹ): bước determinism jar cũ trước, baseline [WIN] lấy từ log
   function-test gần nhất trong ~/claudedata trên Oracle (grep "\[WIN"); rồi jar binance-futures-task112.jar env mới.
   Diff từng dòng [WIN]. Khớp 100% → ghi Kết quả vào tasks/112 + commit. Lệch → STOP.
4. **Chờ marker /d/claudedata/CCD113_CODE_DONE** (nếu chưa có, poll 30 phút; có CCD113_NEEDS_HUMAN → STOP).
   Build jar (JAVA_HOME=/c/Users/pc/.jdks/corretto-17.0.9 /c/Users/pc/bin/mvn -q -DskipTests package),
   md5 2 đầu, scp → Oracle ~/java/simulator/binance-futures-task113.jar. GATE-113 tầng 2 theo tasks/113
   (reset 4 window N=3, SUCCESS khớp 100% jar112, LOW_TRADES hiện diag_* ≠0, verdict flag không đổi).
   PASS → ghi Kết quả tasks/113 + commit. FAIL → STOP.
5. **Launch vế A** (pre-register docs/insights/WFO_PREREG_3WAY_V41.md): reset jobstore full 17 window N=30,
   2 worker detached jar task113 env WFO_SMART_CACHE=1 WFO_DATA_DIR=/home/ubuntu/claudedata/wfo_dataset_wf,
   log ~/claudedata/wfo_v41_A_w{1,2}.log. Ghi lệnh + pid vào báo cáo sáng. DỪNG tại đây (vế B/C sáng bàn).
