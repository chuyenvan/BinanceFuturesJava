# RUNBOOK LIVE (242) — vận hành + DEPLOY bot live (rev5, 2026-08-22)
Dùng lại cho mọi việc đụng live: truy cập, đọc log, DEPLOY trading+ingestor, shadow-mode, rollback, an toàn, PARITY CHECK.
Tất cả lệnh dưới đã CHẠY THẬT (19-22/08). READ-ONLY trừ khi user duyệt thao tác ghi. Live = tiền thật.

## 0. Topology (production subnet 103.157.218.x)
- **LIVE box 242**: `103.157.218.242` (hostname 3stech.vn), **port 2222, user root**, key `id_rsa_chuyennd`.
  ⚠️ IP=103.157.218.242, PORT=**2222** (KHÔNG phải 161.118.212.242, KHÔNG port 222/22). RAM box ~7G (chật, ~0 free). **242 KHÔNG có python3/jq/aql/asinfo** — parse JSON bằng awk.
- **Oracle compute/archive**: `161.118.212.3` (ubuntu, cùng key). tz=GMT+7. Aerospike **dockerized** (container `aerospike-wfo`, port 3222, ns `test`+`ticker`). box TEST single-node, gánh WFO nặng → không HA. Có python3, javac 11, jar.
- **226 (`103.157.218.226`)**: trước báo CHẾT nhưng THỰC TẾ VẪN SỐNG & còn dùng — ns=`ticker` chứa kline_1m_opt/predictions (worker Kaggle aerospike-mode đọc từ đây). Khác kho Oracle ns=test. (đính chính rev5)
- **Aerospike**: bot đọc ns=`ticker` @242:3222; OI history full @Oracle. Redis cluster 242:30001-30006.
- **Bridge điều khiển (rev5)**: desktop-commander trên máy Windows. Cả Oracle lẫn 242 SSH được bằng **Git ssh.exe**: `C:\PROGRA~1\Git\usr\bin\ssh.exe -i C:\Users\pc\.ssh\id_rsa_chuyennd [-p 2222] user@host "tr -d '\r' | bash -s" < C:\Users\pc\script.sh` (pipe script file qua stdin — TRÁNH inline `$` vì cmd/powershell nuốt). `wsl bash -lc "ssh ..."` cũng dùng được (key /root/.ssh/id_rsa_chuyennd trong WSL). ⚠️ `device_bash` (Linux VM sandbox) KHÔNG có route tới Oracle/242 — chỉ đọc source ~/mnt/BinanceFuturesJava. MCP tool cap ~60s/call → việc lâu dùng nohup+poll.

### SSH mẫu (Git ssh.exe qua desktop-commander, shell=cmd)
- Live 242: `C:\PROGRA~1\Git\usr\bin\ssh.exe -i C:\Users\pc\.ssh\id_rsa_chuyennd -p 2222 -o StrictHostKeyChecking=no root@103.157.218.242 "..."`
- Oracle:   `C:\PROGRA~1\Git\usr\bin\ssh.exe -i C:\Users\pc\.ssh\id_rsa_chuyennd -o StrictHostKeyChecking=no ubuntu@161.118.212.3 "..."`

## 1. Hai app trên 242
### A) TRADING bot
- Dir `/home/chuyennd/java/v_t_m` (cwd). Class `com.binance.chuyennd.trading.BinanceOrderTradingManager`. Jar `target/binance-java-sdk-1.2.4.jar`. Log `logs/full.log` (format ts **dd/MM/yyyy HH:MM:SS.mmm**). pidfile `run/<class>.pid`.
- Launcher `bin/daemon.sh {start|stop|restart}` → source `conf/env.sh` → `bin/start.sh` → `java -server -Xms5g -Xmx5g -cp target/... <MAIN>`.
- Model gate/selector: `../storage/ai_ml_data/ai_models_reg_v3/`. Auto-restart ThreadAutoRestartProgram ~4h (in-JVM, GIỮ env).
### B) INGESTOR
- Dir `/home/chuyennd/java/collectData`. Class `...websocket.BinanceDataIngestor`. Xmx2g. env riêng (`AEROSPIKE_READ_CLUSTER=242`). OI-Feat-Compute-Loop `ComputeOiFeat2Live242.runOnce` cadence 60'.

## 2. conf/env.sh (v_t_m) — ENV thật
```
export SELECTOR_RANK_TOPK=5
export SIM_MIN_MOMENTUM_15M=0.008
export SIM_RATE_PROFIT_STOP_MARKET=0.05   # exit thật (config.properties 0.01 BỊ env đè)
export TS_PRED_GAP=1                       # pred-gap ON
export SIM_TS_PROFIT_MULTIPLIER=3.0        # ~arm profit multiplier
export SHADOW_NO_PUSH=false                # REAL từ 21/08 16:30 (trước đó shadow)
```
⚠️ SIM_* CHỈ ở env.sh. Restart PHẢI qua `daemon.sh` (source env.sh). Sau restart verify: `xargs -0 -n1 -a /proc/<pid>/environ | grep -E "SIM_|SELECTOR|SHADOW|OI_|TS_"`.

## 3. DEPLOY jar (đã chạy 19-21/08)
Source Windows `E:\educa\source\github\20260415\BinanceFuturesJava` (= WSL `/mnt/e/...`). Deployable = shaded fat jar `target/binance-java-sdk-1.2.4.jar` (~99.6MB).
- Build: `cd /d E:\...\BinanceFuturesJava && C:\Users\pc\bin\mvn.cmd -DskipTests clean package` (desktop-commander shell=cmd, ~50s, verify BUILD SUCCESS). mvn CHỈ ở `C:\Users\pc\bin\mvn.cmd`.
- Sửa source: desktop-commander edit_block/write_file path `E:\...`. Backup .bak trước.
- Deploy: scp jar → 242 /tmp; cp backup .bak_<tag> rồi cp đè `<dir>/target/`. Ghi đè KHÔNG ảnh hưởng process tới khi restart.
- Restart: `cd <dir>; bin/daemon.sh restart`. Verify pid/env/log.
- (Cách khác nhét 1 class không cần build cả: javac --release 11 -cp <shadedjar> class.java; jar uf <shadedjar> pkg/Class.class — đã dùng cho ClientSingleton/exchange_info.)

## 4. SHADOW-MODE — hiện TẮT (real từ 21/08 16:30)
Guard `processOrderNewMarketNew`: SHADOW_NO_PUSH==true → LOG `[SHADOW] would-BUY` + return. Bật lại = env true + daemon restart.

## 5. ROLLBACK
- Shadow: env SHADOW_NO_PUSH=true + restart. Jar: cp .bak_<tag> + restart. env/config: cp .bak + restart.

## 6. Signature log (grep)
- ENTRY thật: `BUY <SYM> entry: <px> quantity: ... market level: PREDICT_SYMBOL_TRADE` (KHÔNG có [SHADOW]).
- Shadow: `[SHADOW] would-BUY ...`. Vị thế: `Update all position:N`.
- OI-feat: `ComputeOiFeat2Live242 START/DONE ... mode=`. Guard OI: `[OI-GUARD-1] ABORT`, `[OI-GUARD-2] ... GATE`.
- Trailing exit: `Create Stop Loss Algo <SYM> qty ... price ...` (STOP_MARKET treo sàn, trailing theo lãi). Dedup/close: `Remove symbol trade success [<SYM>]`.

## 7-9 (rev3): trạng thái verify, gotchas, an toàn. Bổ sung: `xargs -0 -n1 -a /proc/PID/environ`.

## 10. OI MIGRATION 226→Oracle + GUARDS + C1 accumulator (20/08)
Chi tiết: `claude/oi_migration_226_to_oracle_guards_2026-08-20.md`. Tóm: repoint AEROSPIKE_HOST_226=161.118.212.3 (LIVE 20/08). Guard-1 (compute abort khi Oracle down), Guard-2 (bot GATE khi oi_feat >2h). C1 accumulator CODE XONG PARITY OK (maxDiffZ=0) nhưng CHƯA CUTOVER (env `OI_USE_ACCUM=1` + SEED trước; CHỜ user duyệt).

## 11. LIVE↔BACKTEST PARITY CHECK — tool/quy trình (rev5, đã chạy 21-22/08)
Mục đích: định kỳ soi lệnh live 24h có vênh logic backtest không. READ-ONLY. Docs kết quả: `live_audit_first4h_realorders_2026-08-21`, `live_audit_24h_2026-08-22`.

**Cách chạy:** viết script .sh (write_file → C:\Users\pc\_x.sh), chạy: `C:\PROGRA~1\Git\usr\bin\ssh.exe -i C:\Users\pc\.ssh\id_rsa_chuyennd -p 2222 -o StrictHostKeyChecking=no root@103.157.218.242 "tr -d '\r' | bash -s" < C:\Users\pc\_x.sh`. LOG=/home/chuyennd/java/v_t_m/logs/full.log.

**Các kiểm (grep, lọc ngày `^dd/08/2026`):**
1. Real entries: `grep 'market level: PREDICT_SYMBOL_TRADE' LOG | grep -av 'would-BUY'` (đếm/ngày). Shadow: `grep 'would-BUY'` (mong 0 khi real).
2. **Cap ≤K5**: real entries → `grep -aoE '^dd/08/2026 [0-9]{2}:[0-9]{2}' | sort | uniq -c | sort -rn` → flag >5/tick. (backtest cap-then-skip top-K).
3. Exceptions: `grep -aE 'Exception|ERROR' | grep -avE 'RequestHandler|GET|POST|fapi'`. Chú ý `-4164 min-notional` (backtest KHÔNG model reject → divergence nhỏ).
4. DCA: `grep -ac DCA_LEVEL`. Positions: `grep 'Update all position:' | tail`. OI-GUARD: `grep -ac OI-GUARD`.
5. **Price parity** (cốt lõi): entry px PHẢI = Binance 1m **close nến vừa đóng TRƯỚC ts log** (ts+07 → −7h = UTC; entry log HH:MM:ss dùng close phút HH:(MM−1) thường). 242 KHÔNG python3/jq → curl + awk:
```
s=$(date -u -d "2026-08-22 10:42:00" +%s)000
curl -s "https://fapi.binance.com/fapi/v1/klines?symbol=ACEUSDT&interval=1m&startTime=$s&endTime=$((s+540000))&limit=10" \
 | tr '[' '\n' | grep -aE '^[0-9]' | awk -F',' '{ot=$1/1000;c=$5;gsub(/"/,"",c);"date -u -d @"ot" +%H:%M"|getline hm;printf "%sUTC close=%s\n",hm,c}'
```
So close với entry px (khớp từng chữ số = 0% slippage = đúng backtest).

**Kết quả 22/08 (24h):** real 21/08=55 22/08=40, shadow 24h=0, cap max 3/tick (0 vi phạm), price parity 4/4 KHỚP TUYỆT ĐỐI, 1 exception (BCH −4164), DCA 123, positions 77→94, OI-GUARD 0. → cơ chế khớp backtest, KHÔNG bug mới.

**Vênh còn TỒN (không phải bug mới, cần xử):**
- Baseline backtest chưa apples-to-apples (live arm15+predgap vs canonical arm26) → chưa reconcile P&L số.
- Concurrent positions 94 chưa đo backtest max (open item) + CHƯA có disaster-exit → phình.
- Min-notional reject không model trong backtest.
- DCA-down mạnh (123/24h) averaging vào loser (collapse-risk).
