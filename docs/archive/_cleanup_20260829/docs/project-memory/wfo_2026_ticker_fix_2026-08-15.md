# Fix 2026 ticker lên Kaggle + cold test — 2026-08-15

## Blocker & cách giải (quan trọng, để không lặp lại)

### 1. Cloud container CHẶN outbound port 22
Session cloud mới không SSH thẳng Oracle được (github.com:22 cũng timeout; chỉ 443 thông). device_bash (VM Linux của user) không có network. → PHẢI route qua **Windows git-ssh** bằng desktop-commander start_process:
`& "C:\Program Files\Git\usr\bin\ssh.exe" -p 22 -o StrictHostKeyChecking=no -o PubkeyAcceptedAlgorithms=+ssh-rsa -o IdentitiesOnly=yes -i "C:\Users\pc\.ssh\id_rsa_chuyennd_openssh" ubuntu@161.118.212.3 "<bash>"`.
Lệnh bash phức tạp: base64-encode trong PowerShell (`@'...'@` here-string) → `echo <b64> | base64 -d | bash` (PowerShell nuốt `$()` nếu để trần).

### 2. Kaggle TỰ GIẢI NÉN .tar → file 0-byte (bẫy 1)
Upload `ticker_all.tar` (đuôi .tar) lên hpo-ticker-daily: Kaggle unpack tar server-side theo đuôi, tạo 2050 entry **0 byte** (tải về = 0.00B). Version "ready" nhưng data RỖNG. Đây là lý do v7/v8 "processing chậm 45%".
**GIẢI:** upload tar dưới tên `ticker_bundle.dat` (đuôi opaque) → Kaggle KHÔNG unpack → giữ nguyên khối. Version 9 finalize <1 phút. run_worker tự `tar xf ticker_bundle.dat`.
- Bài học: KHÔNG upload .tar/.zip lên Kaggle nếu muốn giữ content — Kaggle auto-extract. Dùng đuôi opaque + tự giải nén ở kernel.
- Quota KHÔNG phải nguyên nhân (~31GB < ~100GB).

### 3. TAR CHỨA SYMLINK DANGLING → 2021-2025 rỗng (bẫy 2, làm fanout G015x26d FAIL)
Thư mục `/home/ubuntu/java/simulator/kaggle_data_hpo/` dùng symlink: **1886 file 2021-2025 ở top-level chỉ là SYMLINK trỏ vào `daily/`**, 164 file 2026 mới là file thật. `tar cf ticker_*.bin.gz` **archive symlink** (không follow) và KHÔNG kèm `daily/` → trên Kaggle, 2021-2025 giải nén ra symlink dangling (rỗng). run_worker glob đếm 2050 (symlink cũng tính) → qua check ≥1800 → SIM chạy → FAIL-FAST "khong co ticker ngay 2021xxxx tu nguon file" cho đúng 17 window 2021Q1-2025Q1. Extraction/.dat/reader ĐỀU ĐÚNG — chỉ nội dung tar sai.
Chẩn đoán: `tar tvf ticker_all.tar | awk '{print substr($1,1,1)}' | sort|uniq -c` → thấy `l` (symlink) nhiều. File thật ở `daily/ticker_<date>.bin.gz` (vd 20210101 = 2MB gz, gunzip ra 5.72MB = đúng format v6).
**GIẢI:** rebuild `tar -chf` (`-h`/`--dereference` → lưu data thật): `cd kaggle_data_hpo && tar -chf /home/ubuntu/tickertar/ticker_all.tar ticker_*.bin.gz`. Kết quả: **2050 file thật, 0 symlink, ~14.1G** (data thật to hơn bản symlink-broken 2.5G nhiều). Upload lại làm **version 10**.
- LƯU Ý: tên `.bin.gz` là single-gzip; loader KaggleDataLoader.loadDailyTickersShort → loadObject thử cả `kaggle_data_hpo/ticker_<date>.bin` VÀ `.bin.gz` (const pool xác nhận), đọc .gz OK. KHÔNG cần gunzip.

### 4. run_worker patch
5 kernel: dòng glob thêm ticker_bundle.dat:
`tar_hits = sorted(glob.glob(IN + "/**/ticker_all.tar", recursive=True) + glob.glob(IN + "/**/ticker_bundle.dat", recursive=True))`
**md5 = f1c781ee0bbf**. Config K5/moveSL0.05 giữ nguyên.

### 5. RỦI RO DISK KAGGLE (14G tar)
tar giờ 14G → giải nén vào /kaggle/working (~19.5G cap) rất sát, và /working bị commit thành output. Nếu worker fail "no space": sửa run_worker cho extract vào `/kaggle/temp/kaggle_data_hpo` rồi symlink `/kaggle/working/kaggle_data_hpo -> /kaggle/temp/...` (loader đọc relative từ CWD=/kaggle/working; /working output không ôm 14G).

## Cold test 2026
- Lần 1: tag G015x26d → FAIL (bẫy 3, symlink). Lần 2: tag **G015x26e** với tar v10 data thật.
- predwf hardlink từ predwf_G015x26c (18 window, có 2026Q1=20260101 + 2026Q2=20260401 — prediction thật, KHÔNG phụ thuộc ticker).
- Config canonical **G015-K5** (15m/thr0.015/moveSL0.05/K5), WFO_MAX_OOS_DATE=20261001, jobstore 127.0.0.1:3222 ns=test.
- COLD TEST (phương án a): 2026 holdout sạch, KHÔNG re-tune/re-sweep. Report: `oracle_worker_cwd/docs/reports/wfo_strategy_window.md`.
- Verify: DONE hết window, 2026Q1/Q2 vào lệnh + có pnl. Kết luận: 2026 giữ edge → go-live OK; vỡ → edge là artifact regime/selection.

## Pipeline drive_exp18 (tóm)
predwf_$TAG (≥18 predict_wf) → ExportWfoDataset Xmx18g build wfo_ds_$TAG (đọc market/pred/funding từ Aerospike) → `datasets create; datasets version` wfo-ds-$TAG (2 bước → luôn ra v2, KHÔNG phải duplicate) → sed kernel-metadata → WfoCoordinator reset (max_oos 20261001) → push 5 worker → poll DONE. Worker attach hpo-ticker-daily(.dat)+wfo-ds-$TAG+java-run-lc, extract ticker, join jobstore, sim. Lỗi per-window đọc bằng `WfoCoordinator status strategy_window`. Log worker Kaggle: API `/api/v1/kernels/output?userName=chuyendinh&kernelSlug=wfo-worker-N` (field log).
