# REPORT 152 [TEAM FAIL] — Tối ưu xử lý lệnh thua (DCA/SL)

## Pre-register (ghi TRƯỚC khi nhìn số)
- Đạt nếu: max-holding thật ≤ 1 năm (hoặc rõ tỉ lệ vượt), maxDD/cụm không xấu đi so hiện trạng, tỉ lệ-cứu
  (lỗ→hồi) đo được, CAGR full-history không giảm quá 15% so baseline (~4.2%/năm theo report 141).
- Baseline cố định 2 phần kia (đã chốt 2026-07-10/11): `wfo_dataset_v4` (ret2), `TS_GIVEBACK_RATIO=1.0`.

## Bối cảnh đã đọc trước khi làm (tránh lặp việc)
- `docs/MASTER_STRATEGY_CAMPAIGN.md` + `docs/STRATEGY_FINDINGS_20260710.md`: SL cứng mọi độ sâu / time-stop
  24-72h đã đo NET ÂM (loại). Time-stop 270/360 ngày (task 142) không đổi balance → nghi ngờ chưa có cụm
  nào thật sự kẹt >270 ngày ở cấu hình khuyến nghị — task 142 tự ghi "cần verify max-holding trực tiếp",
  đây chính là việc B1 của task này.
- Tier mapping **ĐÃ TỒN TẠI** (khác giả định trong task 143/152!): `CoinRankManager` (tradecore) có
  `CoinTier{TIER_1_BLUECHIP,TIER_2_MIDCAP,TIER_3_SHITCOIN}` + static-tier loader (`ExportCoinTierStatic`,
  `Configs.WFO_STATIC_RANK`). Đã WIRE SẴN vào engine (`SimulatorMarketLevelTicker1MStopLoss:698-716`,
  `DetectEntrySignal2TradeNormal:486-490`, code CŨ — không thuộc diff phiên này): Tier-3 (shitcoin) bị
  CHẶN nhồi `DCA_LEVEL1`; tier multiplier (1.20/1.00/0.50) nhân vào budget sizing. → "tier-based DCA
  stop" của mục 3 trong scope **ĐÃ CHẠY SẴN trong baseline**, không phải NEEDS_HUMAN như presumed.
- 3 task song song cùng phiên (chốt bởi Desktop 2026-07-11): 150=team_entry, 151=team_success,
  152=team_fail (task này). Mỗi team cách ly thư mục riêng trên Oracle, DÙNG CHUNG jar đã build sẵn
  (`/home/ubuntu/java/simulator/binance-futures-backfill.jar`) — KHÔNG rebuild để tránh 3 team giẫm nhau.
  → mọi phép đo dưới đây dùng jar hiện có, KHÔNG đổi code engine.

## HÀNG RÀO đã áp dụng
- Isolation: `/home/ubuntu/team_fail/simulator/` riêng (jar copy read-only, config/storage/logs riêng).
- RAM-budget: kiểm `free -h` trước khi launch (team_success đã chạy Xmx6g song song) — đủ ≥17GB available
  trước và sau khi launch thêm Xmx6g của mình.
- Retry 1 lần do timeout Aerospike thoáng qua (đọc đồng thời 2 sim) tại 2021-06-07 — kill đúng PID
  (1595820, xác nhận cwd=team_fail trước khi kill), KHÔNG đụng job team khác. Lần 2 chạy ổn định.

## Việc B1: đo hiện trạng holding-time + tỉ lệ-cứu (ĐANG CHẠY / xem RESULT cuối)
Baseline: `WFO_DATA_DIR=wfo_dataset_v4`, `TS_GIVEBACK_RATIO=1.0`, `TIME_STOP_HOURS=0`,
`HARD_STOP_LOSS_RATE=0`, `WRITE_SIM_STORAGE=true`, `SIM_END_DATE=20260601` (full-history 2021-01→2026-05).

Công cụ đo mới (ĐO-ONLY, không đổi hành vi engine, không rebuild jar chung):
`src/main/java/com/binance/chuyennd/research/ClusterFailAnalysisTool.java` — đọc
`storage/OrderTestDone.data`, xuất phân bố holding-time (bucket <1/1-7/7-30/30-90/90-180/180-270/270-365/>365
ngày) theo tổng + theo level, tỉ lệ-cứu (maeLow<priceEntry rồi đóng lãi), top-20 cụm loss-exit PnL âm nhất
(candidate never-recover/delist). Đã compile OK trên Oracle (`javac -cp jar` — không sửa/rebuild jar chung).

**GIỚI HẠN ĐÃ GHI RÕ trong Javadoc tool:** `timeStart` trong `OrderTargetInfoTest` là mốc LEG CUỐI (bị
reset mỗi lần DCA nhồi thêm), KHÔNG phải leg đầu (`clusterFirstLegTime` là field `transient` — KHÔNG
serialize được ra storage). Do đó `holdDays = timeUpdate - timeStart` là CẬN DƯỚI (undercount) cho cụm
bị DCA muộn. Số leg (độ sâu DCA chính xác) KHÔNG lưu trực tiếp trong storage — không đo được tuyệt đối
từ artifact hiện có mà không sửa code (đề xuất ở mục Decisions).

(điền tiếp sau khi sim + tool chạy xong — xem RESULT)

=== RESULT ===
STATUS: (điền cuối)
COMMIT: -
ARTIFACTS: /home/ubuntu/team_fail/simulator/storage/OrderTestDone.data ; /home/ubuntu/team_fail/simulator/analysis/
VERIFY: (điền số)
DECISIONS: (điền)
QUESTIONS: (điền)
=== END ===
