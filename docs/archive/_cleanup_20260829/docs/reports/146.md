# Report — TASK-146: Dọn & hợp nhất tài liệu docs/

## Phát hiện quan trọng trước khi làm (đổi phạm vi thực tế so với mô tả task)

Task mô tả "34 file .md rời rạc" và liệt kê danh sách ARCHIVE/HỢP NHẤT dựa trên một snapshot cũ của
`docs/`. Khi rà thực tế (2026-07-11), phần lớn việc "gộp/archive" **đã được làm ở phiên trước**:
`docs/reference/` và `docs/archive/` đã tồn tại đầy đủ, và 10 file ở root đã là **stub 4 dòng** trỏ sang
đó: `AUDIT_filter_ablation.md`, `BO_CODE_DIGEST.md`, `DEPLOY_242.md`, `PRODUCTION_AUDIT.md`,
`RUNBOOK_kaggle_multi_cpu.md`, `STATUS_RECON.md`, `TRACE_backtest_drift.md`, `_reconcile-report.md`,
`aerospike_242_inventory.md`, `basis_verify.md`. → **Không đụng lại các file này** (đã đúng trạng thái
mục tiêu, làm lại là churn vô ích).

Việc THẬT còn lại khi rà xong chỉ có 3 hạng mục — đã làm cả 3 (xem bảng dưới).

## Bảng file → nhóm → đích

| File gốc | Nhóm | Đích | Hành động |
|---|---|---|---|
| `STRATEGY_FINDINGS_20260710.md` | HỢP NHẤT | `STRATEGY_CONSOLIDATED.md` §Phần A | Nội dung move nguyên vẹn, gốc → stub 2 dòng |
| `MASTER_STRATEGY_CAMPAIGN.md` | HỢP NHẤT | `STRATEGY_CONSOLIDATED.md` §Phần B | Nội dung move nguyên vẹn, gốc → stub 2 dòng |
| `he-thong-kien-truc-trang-thai.md` (mới, staged) | ARCHIVE (snapshot trùng lặp) | `reference/ARCHITECTURE_STATE_SNAPSHOT_20260707.md` | `git mv` (giữ staged history) + stub 3 dòng ở gốc + ghi chú 📸 trùng `architecture.md`/`DATA_STATE.md`/`SESSION_START.md` |
| `SOLUTION_FRAMEWORK_20260711.md` | CANONICAL | giữ nguyên | Chỉ thêm mục lục ở `index.md` §Chiến lược (chưa có trước đó) |
| `STRATEGY_ROADMAP_3PART.md` | CANONICAL | giữ nguyên | Chỉ thêm mục lục ở `index.md` §Chiến lược |
| `index.md` | CANONICAL (router) | — | Thêm section `## Chiến lược` (3 pointer) + 1 dòng reference cho snapshot mới |
| 10 file stub đã có sẵn (liệt kê ở trên) | đã hoàn tất phiên trước | `reference/` | Không đụng — verify đúng trạng thái, không phải làm lại |
| `DEPLOY_242_dot2.md` | task yêu cầu ARCHIVE, **KHÔNG làm** | giữ nguyên (canonical, active) | Xem "Mâu thuẫn" bên dưới |
| `LIB_BINANCE_OLD.md` | task yêu cầu ARCHIVE, **KHÔNG làm** | giữ nguyên (canonical, nợ kỹ thuật active) | Xem "Mâu thuẫn" bên dưới |

Mọi số liệu + kết luận trong 2 file HỢP NHẤT được giữ **nguyên văn 100%** (copy-merge, không tóm tắt lại
số), chỉ gộp trùng đúng 1 đoạn "Mục tiêu chiến dịch" (giống hệt nhau ở cả 2 bản gốc) lên đầu file chung.

## Mâu thuẫn cần Uni xác nhận (KHÔNG tự chọn)

Task liệt kê `DEPLOY_242_dot2.md` và `LIB_BINANCE_OLD.md` vào nhóm ARCHIVE ("cũ/đã thay thế"). Rà thực tế:
- `DEPLOY_242_dot2.md`: runbook đợt-2 **CHỜ user duyệt/bấm**, index.md hiện xếp là active ("Deploy" section,
  không phải archive). Archive file này sẽ khiến runbook đang chờ triển khai bị coi nhầm là đã xong/cũ.
- `LIB_BINANCE_OLD.md`: đánh giá dual-connector, index.md xếp dưới "Nợ kỹ thuật" với ghi chú "chưa quyết"
  (migration dở, chưa ai chốt hướng đi) — không phải tài liệu đã thay thế.

→ Cả hai đều đang ở trạng thái **active/pending**, không phải "cũ/đã thay thế" như mô tả gốc của task. Đề
xuất: GIỮ NGUYÊN vị trí (đã làm vậy), không archive cho tới khi Uni xác nhận 2 việc này đã đóng/superseded
thật.

## Acceptance criteria — đối chiếu

- [x] `docs/index.md` có mục lục đầy đủ cho phần mới đụng tới (Chiến lược + 1 reference mới); mọi link đã
      grep xác nhận không còn tham chiếu tên file cũ nào bị treo (`STRATEGY_FINDINGS_20260710`,
      `MASTER_STRATEGY_CAMPAIGN`, `he-thong-kien-truc-trang-thai` chỉ còn xuất hiện trong chính 3 file đó).
- [~] Số file .md ở docs root: KHÔNG giảm bằng đếm-file thô (quy ước đã có từ trước là giữ **stub tại chỗ**
      thay vì xoá/move-hẳn, để không gãy link — 10 file cũ đã theo mẫu này). Nội dung THỰC/không-trùng-lặp
      đã giảm đúng 3 vụ (2 gộp + 1 archive). Nếu Uni muốn giảm SỐ FILE thật (move stub vào `archive/` thay
      vì để ở root) — đây là thay đổi rộng hơn (đụng path mọi nơi khác đang trỏ vào các file gốc), CẦN xác
      nhận riêng trước khi làm, không tự ý.
- [x] `docs/archive/` không cần thêm gì phiên này (không có file nào đủ điều kiện "cũ/đã thay thế" thật sự
      ngoài 2 cái đang mâu thuẫn ở trên).
- [x] Mỗi file gốc bị gộp/move có stub trỏ đi.
- [ ] `git status` sạch — **CHƯA commit** (task `require_review: true` → để STATUS=REVIEW, Uni xem qua rồi
      quyết định commit).
- [x] `git mv` dùng cho file có history/staged (`he-thong-kien-truc-trang-thai.md`, đang ở trạng thái `A`
      trước khi task bắt đầu → `git mv` giữ nguyên trạng thái staged, không mất). 2 file HỢP NHẤT
      (`STRATEGY_FINDINGS_20260710.md`, `MASTER_STRATEGY_CAMPAIGN.md`) là file **chưa từng commit** (`??`)
      → không có history để giữ, nên chỉnh trực tiếp thành stub (tương đương).

## Quyết định phát sinh

- Không gộp `FINDINGS.md` vào `STRATEGY_CONSOLIDATED.md` dù task liệt kê nó trong nhóm HỢP NHẤT.
  `FINDINGS.md` là "nguồn sự thật" backtest/model, được 6 file khác trỏ vào trực tiếp (`index.md`,
  `ROADMAP.md`, `PIPELINE_PROVENANCE.md`, `reports/funding_fee_audit.md`, `insights/GATE_REDESIGN_IDEAS.md`,
  `reference/_reconcile-report.md`) và nội dung (IC model, filter ablation, circuit breaker...) khác chủ đề
  với 2 file "chiến dịch chiến lược" kia. Gộp nó sẽ phá 6 link + trộn 2 chủ đề khác nhau vào 1 file khổng
  lồ — rủi ro cao, lợi ích thấp. Đây là quyết định có tầm ảnh hưởng nên báo lại thay vì tự làm theo mô tả
  gốc (theo luật phản biện ở CORE.md).

=== RESULT ===
STATUS: REVIEW
COMMIT: -
ARTIFACTS: docs/STRATEGY_CONSOLIDATED.md, docs/STRATEGY_FINDINGS_20260710.md (stub), docs/MASTER_STRATEGY_CAMPAIGN.md (stub), docs/reference/ARCHITECTURE_STATE_SNAPSHOT_20260707.md, docs/he-thong-kien-truc-trang-thai.md (stub), docs/index.md
VERIFY: grep xác nhận 0 link chết tới 3 tên file gốc bị move/stub; nội dung 2 file HỢP NHẤT đối chiếu dòng-theo-dòng khớp 100% (copy nguyên văn, chỉ gộp 1 đoạn mục tiêu trùng lặp)
DECISIONS: không gộp FINDINGS.md (khác chủ đề + 6 nơi trỏ vào); không archive DEPLOY_242_dot2.md/LIB_BINANCE_OLD.md (đang active, không phải "cũ/đã thay thế")
QUESTIONS: (1) DEPLOY_242_dot2.md và LIB_BINANCE_OLD.md — Uni xác nhận có còn active không, nếu đã đóng thì archive theo đúng mô tả task; (2) có muốn giảm SỐ FILE thật ở docs root (move 10+ stub cũ vào archive/ luôn) hay giữ quy ước "stub tại chỗ" như hiện tại?
=== END ===
