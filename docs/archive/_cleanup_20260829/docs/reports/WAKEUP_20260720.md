# WAKEUP BRIEF 2026-07-20 (Claude chạy đêm, read-only) — verdict long_softgate + hướng chiến lược
> Ranh giới ca đêm: CHỈ đọc/phân tích. KHÔNG deploy jar, KHÔNG chạy WFO mới, KHÔNG commit, KHÔNG đụng 242. Mọi bước PnL bên dưới CHỜ Uni quyết.

## TRẠNG THÁI HẠ TẦNG (lúc chạy)
- Jobstore RẢNH: WFO 16/16 DONE, 0 RUNNING. Pipeline `wfo_long_full` DONE. `long_softgate` đã xong (jobstore đang giữ nó).
- `ladder_1784297380` treo WAITING_LLM (8/9) — pipeline cũ, để nguyên theo handoff.
- Kaggle: `ev2-export-12h` COMPLETE, `short-crowd-export` COMPLETE → gate cho long_12h và short-WFO đã sẵn (chưa chạy — chờ Uni).

## VERDICT long_softgate = FAIL (nhưng có tin TỐT về sizing)
Pre-reg: %OOS-dương>=70% · WFE_median>=0.5 · maxDD<=50%.
- **%OOS-dương = 18.8% (3/16)** → FAIL. maxDD worst 14.9% (win15), 0/16 margin-call.
- Pattern hoạt động **Y HỆT long_full**: cùng 7 ZERO_TRADES + 5 TOO_FEW. **Soft-gate KHÔNG tạo thêm frequency** — đúng dự đoán (phân bổ lại vốn trong kèo đã admit, không sinh kèo mới).

### Confidence-sizing THỰC SỰ nâng return (điểm đáng giá)
So từng window active (soft-gate vs full, OOS_pnl):

| win | regime | trades | full | softgate | delta |
|---|---|---|---|---|---|
| 1 | 2022Q2 bull | 99 | 640.8 | 919.7 | +44% |
| 3 | 2022Q4 | 37 | 242.8 | 384.7 | +58% |
| 10 | 2024Q3 | 16 | 80.6 | 130.2 | +62% |
| 12 | 2025Q1 | 62 | 1586.8 | 1426.6 | -10% (TOO_MUCH_CAPITAL_LOCK) |
| 15 | 2025Q4 bull | 662 | 6366.0 | 8833.9 | +39% |

- **Tổng OOS_pnl full-cycle: full ~8962 → softgate ~11745 = +31%**, maxDD ngang (win15 15.4%→14.9%, nhẹ hơn), minEq_mtm ngang (~84.6%), 0 margin-call.
- CẢNH BÁO: win12 xấu đi -10% và dính CAPITAL_LOCK = rủi ro concentration đã hiện ở 1 window. Canh khi push size.

## HAI SỰ THẬT ĐÃ ĐÓNG (chuỗi maxdep → softgate)
1. **Hạ ngưỡng (lever A) = chết** (maxdep BURN 8/16, DD 55.6%).
2. **Soft-gate (redistribute) = +31% return, KHÔNG chạm frequency wall.** Sizing lever chạy; frequency thì không.

## CHẨN ĐOÁN CHIẾN LƯỢC (cốt lõi, cần Uni cân)
- Model = **bull-regime momentum harvester**: earner thật CHỈ ở win1/3/12/15 = toàn bull (2022 hồi, 2025). 2023–2024 chop/bear = ZERO gần hết. Gate ngồi im ĐÚNG trong chop.
- ⇒ Tiêu chí "%OOS-dương>=70%" **về bản chất không tương thích** với chiến lược regime-gated đúng-đắn ngồi im nửa chu kỳ. Long-only sẽ KHÔNG BAO GIỜ pass ngưỡng đó.
- **Phép tính lạnh về 20%/năm:** soft-gate +31% → full-cycle CAGR chỉ ~2.9% (long_full ~2.2%). Muốn tới 20% bằng long-only phải scale deployment ~6–7×, dồn vào vài window bull mà win15 đã 15% DD → 6× là vỡ mọi breaker. **Long-only KHÔNG tới 20% ở DD chấp nhận được.**
- ⇒ **Short-crowding đổi vai:** giá trị KHÔNG phải +0.90/kèo, mà là **lấp đúng 7 window ZERO của long (chop 2023–24) = return ở window KHÁC, uncorrelated → nâng CAGR mà KHÔNG chồng DD lên window bull.** Short thành lever cần-thiết-cho-CAGR, không phải biên.

## KHUYẾN NGHỊ (1 hướng, Uni quyết — KHÔNG tự chạy)
Mỗi lần 1 WFO (jobstore rảnh sẵn):
1. **Chốt soft-gate làm baseline long mới** (dominate long_full: +31% cùng DD).
2. **SIZE_MULT sweep NHỎ trên soft-gate** (test path A, bull-concentration): N=2 rồi N=4, KHÔNG nhảy 10; đọc maxDD_mtm + win12 CAPITAL_LOCK; canh BREAKER_MARGIN_HALT=0.50. Caveat cũ "×5 barely moved CAGR" — nếu SIZE_MULT mới (sau-guard) vẫn không nhích → guard/throttle là trần thật.
3. **Ưu tiên song song short-crowding WFO real-path** (short-crowd-export COMPLETE) — đây mới là lever CAGR thật (lấp window chop). Config chốt từ handoff: s60–70 + 24h + q0.95 + veto ls_toptrader top-10%. Cần Uni chọn winner (LLM-gate).
4. Chỉ khi (2)+(3) ổn → multi-sleeve long+short, cross-check 2 tầng Excel (PnL-critical).

## ĐÊM KHÔNG LÀM ĐƯỢC (ghi rõ khỏi tưởng bỏ sót)
- Calibration p6 độc lập: CSV `ev2_preds_n6_2022` **không có cột label** (chỉ p6/p9/oi_z/f8/f14/f22) → không vẽ reliability curve nếu chưa join realized outcome. Nhưng soft-gate WFO đã gián tiếp validate confidence-sizing (SUCCESS window không méo Calmar) → hạ ưu tiên, không phải blocker.
