BẢN ĐỒ HẠ TẦNG CE — Dữ liệu → Mô hình → CE triển khai
Rebuild 2026-08-04 từ code+docs thật (thay thế bản NotebookLM).

CÁCH DÙNG: giải nén cả thư mục rồi mở index.html bằng trình duyệt.
Các file phải nằm cùng thư mục để hyperlink hoạt động.

FILE:
  index.html        Tổng quan + sơ đồ 3 khối + hướng dẫn đọc (người & AI)
  block1-data.html  Topology + 10 tầng dữ liệu + 2 đường đọc ticker + cạm bẫy
  block2-wfo.html   10 tầng luồng WFO end-to-end + Selector/Gate + leak L0-L5
  block3-ce.html    Kiến trúc 5 tầng CE + 40 nút ce.cmd + 16 pipeline + profile
  integrity.html    Preflight Gate 21 lỗi + 14 nguyên tắc + 5 SIMULATOR facts
  glossary.html     Từ điển + index artifact -> tầng -> nút CE
  graph.json        Bản MÁY-ĐỌC (nodes/edges) — id trùng anchor HTML
  assets/style.css  Giao diện dùng chung

MỌI khẳng định neo file:line nguồn. Sửa hành vi code -> cập nhật cả HTML lẫn graph.json.
