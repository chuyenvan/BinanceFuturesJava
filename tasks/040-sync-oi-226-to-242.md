---
id: 040
status: CANCELLED
depends_on: [013]
touches_live_process: false
writes_242_data: true
resource: heavy_226
checkpoint: false
max_retry: 2
report: docs/reports/040.md
require_review: true
---

# TASK-040: Sync OI/LS/taker sets từ Aerospike 226 → 242 (job trên 226 + validate đầy đủ)

- **status:** TODO. `depends_on: [013]` (backfill 226 xong mới sync).
- **Vì sao tách khỏi 013:** Kaggle 5 worker (013) chỉ ghi được **226** (Kaggle không tới 242). 242 là source theo kiến trúc A → cần 1 job chạy **TRÊN 226** (226 thông 242) đẩy sang. Tách ra để validate riêng, không nhồi vào job backfill.

## Việc
Tool `SyncOiSets226To242` — hoặc mở rộng `ReplicateSet242To226` (034) thêm **chiều ngược 226→242** (034 hiện chỉ 242→226):
- Đọc 5 set OI từ Aerospike **226**: `open_interest`, `oi_ls_toptrader_acc`, `oi_ls_toptrader_pos`, `oi_ls_global_acc`, `oi_taker_vol`.
- Ghi sang **242**, **idempotent by digest** (như 034 — không ghi lại key đã khớp).
- **MERGE GUARD bắt buộc:** 242 đã có OI forward realtime (007-C). Sync chỉ **bổ sung/merge lịch sử**, TUYỆT ĐỐI không xoá/đè mất phần forward 242 đang chạy. Bắt chước `writeFundingMap` (merge map, không overwrite rỗng).
- Chạy **TRÊN 226** (SSH). SLF4J. `System.exit(0)` cuối main. Ghi PID/.run, kill đúng PID, dọn xong (CLAUDE.md #6/#7).
- Idempotent → rerun an toàn (sync nhiều lần ra cùng kết quả).

## Validate đầy đủ (require_review — pre-register, PASS hết mới DONE)
1. **Đếm record mỗi set:** sau sync, mỗi symbol `count(242) ≥ count(226)` và `242 ⊇ 226` (242 = history 226 + forward). Không symbol nào thiếu so 226.
2. **Digest sample:** recompute digest vài chục key ngẫu nhiên ở 226 vs 242 → khớp 100%.
3. **KHÔNG mất 242 cũ:** snapshot 242 ở vùng forward (007-C realtime) TRƯỚC và SAU sync → không key/giá trị nào bị xoá hay đè sai. Đây là check quan trọng nhất (merge guard).
4. **Coverage/range:** `[firstSeen, lastSeen]` mỗi symbol ở 242 ⊇ 226; số ngày gap không tăng sau sync.
5. **Cross mốc API:** vài mốc OI ở 242 == 226 == `openInterestHist.sumOpenInterestValue` (đơn vị khớp, đã 0% ở B1/013).

## An toàn
- Chỉ THÊM/merge 5 set OI sang 242. KHÔNG đụng set khác. **KHÔNG deploy/restart process live** (BinanceDataIngestor/BinanceOrderTradingManager) — chỉ ghi data. `writes_242_data` chạy qua SSH 226.
- 013 (226) độc lập với task này: train (018/039 đọc 226) KHÔNG chờ 040; 040 chỉ phục vụ 242-source/live.

## (Code / Kết quả điền)
