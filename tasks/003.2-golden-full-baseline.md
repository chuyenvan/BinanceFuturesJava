# TASK-003.2: Golden FULL baseline — khoá gate liêm chính + mốc chiến lược FULL

- **status:** done (baseline FULL promoted) — `docs/golden/baseline-FULL.json` @ `571b9b2`. Gate liêm chính PASS (FULL totalPnl=68513.79 khớp breaker OFF=68513, lệch <1.0 do double-vs-float).
- **Milestone:** Hạ tầng change-control (ADR-0006). Tiền đề cho mọi thay đổi CHIẾN LƯỢC (breaker, backfill) đo trên FULL.
- **Thực thi bởi:** Claude Code (**Java**). Tool ĐÃ CÓ: `GoldenBacktest` (mode FULL). Đọc cluster 242. ~12'.
- **Quyết định nền:** ADR-0006 (FULL = 2021→2026, fingerprint) · TASK-006 (cần mốc FULL-OFF để khoá gate liêm chính).

## Vì sao cần (TASK-006 lộ ra)
`006` không khoá được gate liêm chính vì **không có mốc FULL-OFF** để so `totalPnl`. Mode OFF của breaker (+68513) chính là FULL-không-phanh — nếu `GoldenBacktest FULL` ra **khớp** số đó (cùng commit) thì: (a) xác nhận OFF/breaker không lệch logic, (b) có `baseline-FULL` làm mốc cho mọi run chiến lược sau.

## Mục tiêu (1 câu)
Chạy `GoldenBacktest FULL` trên commit sạch, **khớp** breaker OFF (68513) → promote `docs/golden/baseline-FULL.json`.

## Luồng (tuần tự, tự cập nhật trạng thái; fail thì retry đúng bước)
- [ ] **B1 — Dọn + sync:** dọn job java cũ của mình (CLAUDE.md), `git pull`. Commit nền nên là `f1f32a3` (commit đã chạy `006`) để so OFF hợp lệ; nếu có thay đổi mới thì commit sạch trước.
- [ ] **B2 — Chạy `GoldenBacktest FULL`** với range **KHỚP** `RunBreakerBacktest` (`20210101→20260601`) để so được với OFF. Pre-flight phải pass (lookahead+slippage+fee>0), `dirty=false`.
- [ ] **B3 — Gate liêm chính:** so `totalPnl` FULL với **breaker OFF = 68513** (cùng commit `f1f32a3`).
  - PASS (khớp) → OFF xác nhận đúng, tool FULL nhất quán → sang B4.
  - **FAIL (lệch):** KHÔNG promote. Ghi số lệch + nghi nhân (range/logic `GoldenBacktest FULL` khác `RunBreakerBacktest` OFF?) vào *(Code điền)*, DỪNG, báo. Retry sau khi tìm nguyên nhân.
- [ ] **B4 — Promote:** `cp` fingerprint → `docs/golden/baseline-FULL.json` → `git add && commit && push`.
- [ ] **B5 — Đóng task:** status = `done (baseline FULL promoted)`; ghi `<sha>` + metric vào *(Code điền)*.

## Lưu ý đọc
`baseline-FULL` số tuyệt đối **lạc quan** (data thiếu coin chết + funding fee tắt). Nó là mốc **regression** (phát hiện trôi code khi sửa), KHÔNG phải bằng chứng chiến lược. Đánh giá chiến lược chỉ đáng tin sau backfill.

## Acceptance criteria
- [ ] `GoldenBacktest FULL` chạy commit sạch (`dirty=false`), range khớp breaker.
- [ ] `totalPnl` khớp breaker OFF (68513) — hoặc nếu lệch, có giải thích + không promote.
- [ ] `docs/golden/baseline-FULL.json` promote + push.
- [ ] Java, SLF4J, KHÔNG System.out.

---
## (Code điền) Kết quả

- Sửa `GoldenBacktest.FULL_END` từ `now()` → cố định **`20260601`** (commit `571b9b2`) — baseline tái lập được + khớp range `RunBreakerBacktest`. Engine KHÔNG đổi. Rebuild + deploy 226.
- **B2** `GoldenBacktest FULL` (20210101→20260601, commit sạch `571b9b2`, dirty=false, qua `BacktestIntegrityGuard`, ~11.5'):
  - totalPnl=**68513.79** · maxDD=−20405.97 · worstSingleLoss=−1414.16 · numTrades=71426 · clustersHeld30d=48 · **nearLiq=1**.
  - PnL/năm: 2021=21517 · 2022=7085 · 2023=9678 · 2024=17948 · 2025=11816 · 2026=469.
- **B3 GATE PASS**: totalPnl FULL=68513.79 vs breaker OFF=68513; **PnL/năm khớp tuyệt đối** (mọi năm trùng khi làm tròn). Lệch tổng <1.0 = GoldenBacktest cộng `double` vs RunBreakerBacktest cộng `float` (sai số tích lũy 71426 trade, ~0.001%), KHÔNG lệch logic ⇒ OFF/breaker xác nhận đúng.
- **B4** promote `docs/golden/baseline-FULL.json` @ `571b9b2` + push.

Thư viện golden hiện có 4 baseline: FAST(RECENT) · FAST_CRASH · FAST_BULL · **FULL**.

## (Code điền) Phát hiện ngoài scope

- **FULL trước đây dùng `now()` → baseline KHÔNG tái lập** (end trôi mỗi lần). Đã sửa cố định. (GoldenBacktest FAST/range khác không dính vì end cố định sẵn.)
- maxDD FULL = −20406 (đáy ở **2025**), nearLiq=1 (1 cụm tụt ≤−90% — sát xoá sổ, dù data thiếu coin chết).

## (Code điền) Quyết định phát sinh

- `baseline-FULL` là mốc **regression** (bắt trôi code), KHÔNG phải bằng chứng chiến lược (số tuyệt đối lạc quan vì thiếu coin chết + funding fee tắt — xem ADR-0007). Mọi thay đổi chiến lược (breaker/backfill) so trên FULL từ mốc này.
